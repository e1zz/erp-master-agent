# agents-skills-installer
Small NPX installer that copies a bundled `skills/` folder into a repository's `.agents/skills` directory.

Usage
Install via npx (when published or using a GitHub URL):
```bash
npx your-username/agents-skills-installer
```
